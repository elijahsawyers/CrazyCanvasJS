/*
    Created by Elijah Sawyers on 03/07/19.

    Abstract:
    This file contains the main functionality of PDFGraphmaticJS.
 */

/*

*/
function main() {
    // Grab the PDF's iFrame, and resize the height to the size of the PDF document.
    const pdfCanvas = document.getElementById("PDFCanvas");
    const context = pdfCanvas.getContext("2d");
    
}

// Start executing after the window loads.
window.onload = main